[pytest]
testpaths = tests
# Put the project root on sys.path so tests import `app` and `scripts` the same
# way the application does. Without this, pytest would only add tests/.
pythonpath = .
addopts = -q --strict-markers
